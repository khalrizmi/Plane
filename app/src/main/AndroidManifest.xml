<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hamz.hamzplane">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity
            android:name=".SearchBandara"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Kalender" />
        <activity
            android:name=".DaftarTerbang"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RingkasanPemesanan"
            android:parentActivityName=".DaftarTerbang" />
        <activity
            android:name=".DataPesan"
            android:parentActivityName=".RingkasanPemesanan" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>